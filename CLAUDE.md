# Claude Notes

Read and follow `AGENTS.md` before doing any work.

In particular:

- Never commit, push, create a PR, or deploy this repository. The owner performs those actions after reviewing for archive-data leaks.
- Never extract or copy a raw social-media archive into the repository.
- Use synthetic fixtures only; never quote private archive payloads.
- Run Python only as `uv run ...`. Do not activate a virtual environment or propose alternate environment setup.
- Use Git Bash-compatible commands and Makefile recipes.

